version: "3.9"
services:
  db:
    image: mysql:8.0
    ports:
      - 3306:3306
    volumes:
      - db-data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
    networks:
      - backend

  backend:
    build:
      context: .
    ports:
      - 8000:8000
    environment:
      - DB_HOST=host.docker.internal
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=root
      - DB_NAME=joinus
      - JWT_SECRET=sunioj
    networks:
      - backend
    depends_on:
      - db

volumes:
  db-data:

networks:
  backend:
